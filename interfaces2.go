package main

/*
import "fmt"

type Storage interface {
	Get() string
	Set(string)
}
type Person struct {
	name string
}

// costructor
func NewPerson(name string) *Person {
	return &Person{name}
}

// getter y setter
func (p *Person) Get() string {
	return p.name
}
func (p *Person) Set(name string) {
	p.name = name
}

func Exec(s Storage, name string) {
	s.Set(name)
	fmt.Println(s.Get())
}
func main() {
	p := NewPerson("Santiao")
	Exec(p, "juan")
}
*/
///difrencia entre metodos de valor y uno de tipo puntero
